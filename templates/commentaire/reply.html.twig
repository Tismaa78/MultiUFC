{% extends 'base.html.twig' %}

{% block title %}Répondre à un commentaire{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2 class="mb-3">Répondre à {{ commentaire.user.nickname }}</h2>

    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button class="btn btn-primary">Envoyer la réponse</button>
    {{ form_end(form) }}
</div>
{% endblock %}
